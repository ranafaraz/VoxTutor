name: Initialize wiki

on:
  workflow_dispatch:

jobs:
  wiki:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Write wiki pages and push
        env:
          GH_WIKI_TOKEN: ${{ secrets.GH_WIKI_TOKEN }}
        run: python .github/scripts/push_wiki.py
